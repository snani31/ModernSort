<UserControl x:Class="ModernSort.View.Pages.SelectedMediaObjectPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModernSort.View.Pages"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:customCtrs ="clr-namespace:ModernSort.CustomeControls.CustomUIControls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Border
        Background="Transparent"
        d:Background="Aqua"
        >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Rectangle
                     Grid.Row="0"
                    Margin="10"
                    RadiusX="20"
                    RadiusY="20"
                    >
                <Rectangle.Fill>
                    <ImageBrush
                Stretch="Fill"
                ImageSource="{Binding ElementName=MediaFilesList, Path=SelectedItem, 
                        Converter={StaticResource ToBitmapConverter}}"/>
                </Rectangle.Fill>
            </Rectangle>
            <Border
                CornerRadius="15 15 20 20"
                Background="{DynamicResource MeinBackgroundColor}"
                Grid.Row="1">
                <Grid
                    >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

                    <customCtrs:CustomButton
                        Grid.Column="0"
                        Command="{Binding ScrollPresenterLeft}"
                        ToolTip="Left scroll"
                        FirstButtonColor="{DynamicResource MeinThemeColor}"
                        SecondButtonColor="{DynamicResource MeinBackgroundColor}"
                        ImageMargin="5"
                        CornerRadius="0 0 0 20"
                        TextBlockHorizontalAlignment ="Left"
                        DefoultImageSource="{DynamicResource TriangleArrowMein}"
                        MouseHoverImageSource="{DynamicResource TriangleArrowBordered}"
                        ImageFlowDirection="RightToLeft"
                        />

                   
                    <Border
                        Grid.Column="1"
                        >
                        <ListView
                            BorderThickness="0"
                            SelectedIndex="{Binding Path=SelectedPathIndex}"
                       ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                       ScrollViewer.VerticalScrollBarVisibility="Disabled"
                       HorizontalContentAlignment="Stretch"
                       VerticalContentAlignment="Stretch"
                       ItemsSource="{Binding MediaFiles}"
                       x:Name="MediaFilesList"
                       Background="Transparent"
                       >
                            <ListView.ItemContainerStyle>
                                <Style TargetType="{x:Type ListViewItem}" >
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                <Border
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="10 5 10 5"
                                                    >
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger 
                                                        Property="IsMouseOver"
                                                        Value="true">
                                                        <Setter 
                                                            Property="Background"
                                                            Value="{DynamicResource DimBackgroundColor}"/>
                                                        <Setter 
                                                            Property="BorderBrush"
                                                            Value="{DynamicResource DimBackgroundColor}"/>
                                                    </Trigger>
                                                    <Trigger 
                                                        Property="IsSelected"
                                                        Value="true">
                                                        <Setter 
                                                            Property="Background"
                                                            Value="{DynamicResource MeinThemeColor}"/>
                                                        <Setter 
                                                            Property="BorderBrush"
                                                            Value="{DynamicResource DimMeinThemeColor}"/>
                                                        <Setter 
                                                            Property="BorderThickness"
                                                            Value="1"/>
                                                    </Trigger>
                                                    </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>

                                <DataTemplate>
                                    <Rectangle
                                     Width="100"
                                     x:Name="racImg"
                                     StrokeThickness="5"
                                     Grid.Row="1"
                                     RadiusX="10"
                                     RadiusY="10"
                                     >
                                        <Rectangle.Fill>
                                            <ImageBrush
                                                Stretch="Fill"
                                                ImageSource="{Binding Path=., Converter={StaticResource ToBitmapConverter}}"/>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                    <DataTemplate.Triggers>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True" />
                                            </MultiTrigger.Conditions>
                                        </MultiTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel
                                        Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Stretch"
                                        />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                        </ListView>
                    </Border>

                    <customCtrs:CustomButton
                        Grid.Column="2"
                        CornerRadius="0 0 20 0"
                        Command="{Binding ScrollPresenterRight}"
                        ToolTip="Left scroll"
                        FirstButtonColor="{DynamicResource MeinThemeColor}"
                        SecondButtonColor="{DynamicResource MeinBackgroundColor}"
                        ImageMargin="5"
                        TextBlockHorizontalAlignment ="Left"
                        DefoultImageSource="{DynamicResource TriangleArrowMein}"
                        MouseHoverImageSource="{DynamicResource TriangleArrowBordered}"
                        />

                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
