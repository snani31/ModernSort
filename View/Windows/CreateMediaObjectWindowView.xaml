<Window x:Class="ModernSort.View.Windows.CreateMediaObjectWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModernSort.View.Windows"
        xmlns:vms="clr-namespace:ModernSort.ViewModel.Windows"
        mc:Ignorable="d"
        Title="CreateMediaObjectWindowView" 
        WindowStyle="none"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        Height="600" 
        Width="1000"
        d:DesignHeight ="1200"
        >
    <Border
    Background= "#f7f9fe"
    CornerRadius="20"
    ClipToBounds="True"
    >
        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition
                Height="50"
                />
                <RowDefinition
                Height="*"
                />
                <RowDefinition
                Height="100"
                />
            </Grid.RowDefinitions>
            <Border
 Grid.Column="0"
 Grid.ColumnSpan="1"
 CornerRadius="20,20,0,0"
 Background="#512f95"
 BorderThickness="0"
 >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="2"
             Command="{Binding CloseDialogCommand}"
             ToolTip="Закрыть">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" Background="Transparent" CornerRadius="10 20 0 10">
                                    <Image Margin="5" Source="\AppResources\PowerOffButtonIcon.png"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="#a1261d"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="1"
             Command="{Binding CollapselicationCommand}"
             ToolTip="Свернуть">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" Background="Transparent" CornerRadius="10 20 0 10">
                                    <Image Margin="5" Source="\AppResources\CollapseWindpwIcon.png"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="#492988"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </Grid>
            </Border>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Border
                    Grid.Column="0"
                    Grid.Row="1"
                    Background="#ccd5f0"
                    CornerRadius="20"
                    Margin="20 20 20 0"
                     >
                     <ScrollViewer
                         Margin="10"
                         Padding="20"
                         >
                        <ScrollViewer.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource WhiteScrollBarStyle}"/>
                        </ScrollViewer.Resources>
                        <StackPanel
                            >
                            <TextBlock
                                TextWrapping="Wrap"
                                Foreground="#7d4c94"
                                FontSize="25"
                                Style="{StaticResource SpanDefoult}"
                                Text="Select tytle for new Media Object"
                                />
                            <TextBlock
                                TextWrapping="Wrap"
                                Margin="10 5 10 0"
                                FontSize="20"
                                Foreground="Red"
                                Text="{Binding ElementName=InputTytle, Path=(Validation.Errors)[0].ErrorContent}"
                                />
                            <TextBox
                                x:Name="InputTytle"
                                FontSize="30"
                                Foreground="#6795de"
                                TextWrapping="Wrap"
                                Background= "#f7f9fe"
                                Style="{StaticResource TuskDataTextBoxStyle}"
                                Text="{Binding Tytle}"
                                Margin="15 5 15 15"
                                Padding="10"
                                />
                            <TextBlock
                                TextWrapping="Wrap"
                                Foreground="#7d4c94"
                                FontSize="25"
                                Style="{StaticResource SpanDefoult}"
                                Text="Write some descriptyons for your new Mediaobject"
                                />
                            <TextBlock
                                TextWrapping="Wrap"
                                Margin="10 5 10 0"
                                FontSize="20"
                                Foreground="Red"
                                Text="{Binding ElementName=InputDescryption, Path=(Validation.Errors)[0].ErrorContent}"
                                />
                            <TextBox
                                x:Name="InputDescryption"
                                FontSize="30"
                                Foreground="#6795de"
                                TextWrapping="Wrap"
                                Background= "#f7f9fe"
                                Text="{Binding Descriptyon}"
                                Style="{StaticResource TuskDataTextBoxStyle}"
                                Margin="15 5 15 15"
                                Padding="10"
                                />
                            <Label
                                Margin="10 5 10 0"
                                FontSize="20"
                                Foreground="Red"
                                Content="{Binding ElementName=MediaFilesList, Path=(Validation.Errors)[0].ErrorContent}"
                                />
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="4*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Grid.Column="1"
                                    TextWrapping="Wrap"
                                    Foreground="#7d4c94"
                                    FontSize="25"
                                    Style="{StaticResource SpanDefoult}"
                                    Text="Select mediafiles"
                                    />
                                <Button
                                    ToolTip="Select picture"
                                    Content="Выбрать"
                                    Height="60"
                                    Grid.Column="0"
                                    Command="{Binding SelectMultimediaFiles}"
                                    >
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border BorderThickness="0" Margin="0" Name="brd" Background="Transparent" CornerRadius="10 20 0 10">
                                                <Image x:Name="icon" Margin="5" Source="\AppResources\Pictures\addPurple.png"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="brd" Property="Background" Value="#512f95"/>
                                                    <Setter TargetName="icon" Property="Source" Value="\AppResources\Pictures\addWhite.png" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                            <Border 
                                BorderBrush="#512f95"
                                BorderThickness="3"
                                CornerRadius="20"
                                Height="300"
                                Padding="10"
                                Background="#f7f9fe"
                                >
                                <ListView
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    HorizontalContentAlignment="Center"
                                    x:Name="MediaFilesList"
                                    ItemContainerStyle="{StaticResource MediaObjactContenerStyle}"
                                    ItemsSource="{Binding SelectedFiles}"
                                    BorderThickness="0"
                                    Background="Transparent"
                                    Padding="0 10 0 0"
                                    >
                                    <ListView.Style>
                                        <Style>
                                            <Style.Setters>
                                                <Setter Property="Validation.ErrorTemplate">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <Border
                                                                CornerRadius="15"
                                                                BorderBrush="#7d4c94" 
                                                                BorderThickness="2"
                                                                >
                                                                <AdornedElementPlaceholder />
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style.Setters>
                                        </Style>
                                    </ListView.Style>
                                    <ListView.ItemTemplate>
                                        
                                        <DataTemplate>
                                            <Border
                                                Background="Transparent"
                                                x:Name="brd" 
                                                Width="300" 
                                                MinHeight="400" 
                                                CornerRadius="10">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="50"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>
                                                    <Border 
                                                        x:Name="InBrd"
                                                        Grid.Row="0"
                                                        CornerRadius="10 10 0 0"
                                                        Background="#7947e1">
                                                        <StackPanel
                                                            Orientation="Horizontal"
                                                            HorizontalAlignment="Right"
                                                            >
                                                            <Button
                                                                ToolTip="Remove media"
                                                                Content="Выбрать"
                                                                Height="50"
                                                                Grid.Column="0"
                                                                Command="{Binding RemoveMediafile}"
                                                                CommandParameter="{Binding}"
                                                                >
                                                                <Button.Template>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border 
                                                                            BorderThickness="0" 
                                                                            Margin="0" Name="brd" 
                                                                            Background="Transparent">
                                                                            <Image x:Name="icon" 
                                                                                   Margin="5" 
                                                                                   Source="\AppResources\Pictures\redCrossBorder.png"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter TargetName="brd" Property="Background" Value="#653bbb"/>
                                                                                <Setter TargetName="brd" Property="CornerRadius" Value="0 10 0 0"/>
                                                                                <Setter TargetName="icon" Property="Source" Value="\AppResources\Pictures\redCrossFill.png" />
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Button.Template>
                                                            </Button>
                                                        </StackPanel>
                                                    </Border>
                                                    <Rectangle
                                                        x:Name="racImg"
                                                        Stroke="Red"
                                                        StrokeThickness="5"
                                                        Margin="10"
                                                        Grid.Row="1"
                                                        RadiusX="20"
                                                        RadiusY="20"
                                                        >
                                                        <Rectangle.Fill>
                                                            <ImageBrush
                                                                Stretch="Fill"
                                                                ImageSource="{Binding MediaImagePath}"/>
                                                        </Rectangle.Fill>
                                                    </Rectangle>
                                                </Grid>
                                            </Border>
                                            <DataTemplate.Triggers>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsMouseOver" Value="True" />
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="brd" Property="Background" Value="#653bbb" />
                                                    <Setter TargetName="racImg" Property="StrokeThickness" Value="0" />
                                                </MultiTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.Resources>
                                        <Style TargetType="ScrollBar" BasedOn="{StaticResource WhiteScrollBarStyle}"/>
                                    </ListView.Resources>
                                    <ListView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel 
                                                Orientation="Horizontal"  
                                                HorizontalAlignment="Center"
                                                />
                                        </ItemsPanelTemplate>
                                    </ListView.ItemsPanel>
                                </ListView>

                            </Border>

                        </StackPanel>
                    </ScrollViewer>
                </Border>
                <Border 
                    Grid.Column="1"
                    Background="#0b4d73"
                    >
                    <TextBlock
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Text="Filters coming soon"
                        />
                </Border>
            </Grid>
           
            
            <Button
                Grid.Row="2"
                Content="Create new MediaObject"
                Margin="20"
                Background="#512f95"
                Style="{StaticResource Button}"
                Command="{Binding CreateMediaObjectCommand}"
                        />
        </Grid>
    </Border>
</Window>
