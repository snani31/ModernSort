<customCtrs:BaseWindow x:Class="ModernSort.View.Windows.CreateMediaObjectWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModernSort.View.Windows"
        xmlns:vms="clr-namespace:ModernSort.ViewModel.Windows"
        xmlns:customCtrs ="clr-namespace:ModernSort.CustomeControls.CustomUIControls"
        mc:Ignorable="d"
        Title="CreateMediaObjectWindowView" 
        WindowStyle="none"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        Height="620" 
        Width="1000"
        d:DesignHeight ="1200"
        MouseLeftButtonDown="Window_DragHandler"
        >
    <Border
    Background= "{DynamicResource MeinBackgroundColor}"
    CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=customCtrs:BaseWindow,Mode=FindAncestor},Path=MainBorderCornerRadius}"
    ClipToBounds="True"
    >
        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition
                Height="50"
                />
                <RowDefinition
                Height="*"
                />
                <RowDefinition
                Height="120"
                />
            </Grid.RowDefinitions>
            <Border
                Grid.Column="0"
                Grid.ColumnSpan="1"
                d:CornerRadius="20,20,0,0"
                CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=customCtrs:BaseWindow,Mode=FindAncestor},Path=TopButtonsBarBorderCornerRadius}"
                Background="{DynamicResource MeinThemeColor}"
                BorderThickness="0"
                >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>


                    <Button Grid.Column="1"
         ToolTip="Закрыть"
            Click="HideButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" Background="Transparent" CornerRadius="10 20 0 10">
                                    <Image Margin="8" Source="{DynamicResource MinimizeBordered}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="{DynamicResource DimMeinThemeColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2"
         ToolTip="Свернуть"
            Click="MaximizeButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" Background="Transparent" CornerRadius="10 20 0 10">
                                    <Image Margin="8" Source="{DynamicResource MaximizeBordered}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="{DynamicResource DimMeinThemeColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="3"
        Command="{Binding CloseDialogCommand}"
        ToolTip="Закрыть">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" Background="Transparent" CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=customCtrs:BaseWindow,Mode=FindAncestor},Path=TopRightCloseButtonBorderCornerRadius}">
                                    <Image Margin="10" Source="{DynamicResource PowerOffMeinBackgroundImage}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="{DynamicResource ContrastingColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </Grid>
            </Border>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Border
                    Grid.Column="0"
                    Grid.Row="1"
                    Background="{DynamicResource DimBackgroundColor}"
                    CornerRadius="20"
                    Margin="20 20 20 0"
                     >
                     <ScrollViewer
                         Margin="10"
                         Padding="20"
                         >
                        <ScrollViewer.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource WhiteScrollBarStyle}"/>
                        </ScrollViewer.Resources>
                        <StackPanel
                            >
                            <TextBlock
                                TextWrapping="Wrap"
                                Foreground="{DynamicResource MeinThemeColor}"
                                FontSize="25"
                                Style="{StaticResource SpanDefoult}"
                                Text="Select tytle for new Media Object"
                                />
                            <TextBlock
                                TextWrapping="Wrap"
                                Margin="10 5 10 0"
                                FontSize="20"
                                Foreground="{DynamicResource ContrastingColor}"
                                Text="{Binding ElementName=InputTytle, Path=(Validation.Errors)[0].ErrorContent}"
                                />
                            <TextBox
                                x:Name="InputTytle"
                                FontSize="30"
                                Foreground="{DynamicResource MeinThemeColor2}"
                                TextWrapping="Wrap"
                                Background= "{DynamicResource MeinBackgroundColor}"
                                Style="{StaticResource TuskDataTextBoxStyle}"
                                Text="{Binding Tytle}"
                                Margin="15 5 15 15"
                                Padding="10"
                                />
                            <TextBlock
                                TextWrapping="Wrap"
                                Foreground="{DynamicResource MeinThemeColor}"
                                FontSize="25"
                                Style="{StaticResource SpanDefoult}"
                                Text="Write some descriptyons for your new Mediaobject"
                                />
                            <TextBlock
                                TextWrapping="Wrap"
                                Margin="10 5 10 0"
                                FontSize="20"
                                Foreground="{DynamicResource ContrastingColor}"
                                Text="{Binding ElementName=InputDescryption, Path=(Validation.Errors)[0].ErrorContent}"
                                />
                            <TextBox
                                x:Name="InputDescryption"
                                FontSize="30"
                                Foreground="{DynamicResource MeinThemeColor2}"
                                TextWrapping="Wrap"
                                Background= "{DynamicResource MeinBackgroundColor}"
                                Text="{Binding Descriptyon}"
                                Style="{StaticResource TuskDataTextBoxStyle}"
                                Margin="15 5 15 15"
                                Padding="10"
                                />
                            <Label
                                Margin="10 5 10 0"
                                FontSize="20"
                                Foreground="{DynamicResource ContrastingColor}"
                                Content="{Binding ElementName=MediaFilesList, Path=(Validation.Errors)[0].ErrorContent}"
                                />
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Grid.Column="1"
                                    TextWrapping="Wrap"
                                    Foreground="{DynamicResource MeinThemeColor}"
                                    FontSize="30"
                                    Style="{StaticResource SpanDefoult}"
                                    Text="Select mediafiles"
                                    Margin="0 0 60 10"
                                    />

                                <customCtrs:CustomButton
                                    ToolTip="Select picture"
                                    Command="{Binding SelectMultimediaFiles}"
                                    Height="60"
                                    Grid.Column="0"
                                    FirstButtonColor="{DynamicResource MeinThemeColor}"
                                    SecondButtonColor="{DynamicResource DimBackgroundColor}"
                                    CornerRadius="10 0 0 0"
                                    ImageMargin="5"
                                    TextBlockHorizontalAlignment ="Left"
                                    DefoultImageSource="{DynamicResource AddPlusMein}"
                                    MouseHoverImageSource="{DynamicResource AddPlusBordered}"
                                    />

                            </Grid>
                            <Border 
                                BorderBrush="{DynamicResource MeinThemeColor}"
                                BorderThickness="3"
                                CornerRadius="0 20 20 20"
                                MinHeight="300"
                                MaxHeight="500"
                                Background="{DynamicResource MeinBackgroundColor}"
                                >
                                <ListView
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    HorizontalContentAlignment="Center"
                                    x:Name="MediaFilesList"
                                    ItemContainerStyle="{StaticResource MediaObjactContenerStyle}"
                                    ItemsSource="{Binding SelectedFiles}"
                                    BorderThickness="0"
                                    Background="Transparent"
                                    Padding="0 10 0 0"
                                    >
                                    <ListView.Style>
                                        <Style>
                                            <Style.Setters>
                                                <Setter Property="Validation.ErrorTemplate">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <Border
                                                                CornerRadius="0 20 20 20"
                                                                BorderBrush="{DynamicResource ContrastingColor}" 
                                                                BorderThickness="3"
                                                                >
                                                                <AdornedElementPlaceholder />
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style.Setters>
                                        </Style>
                                    </ListView.Style>
                                    <ListView.ItemTemplate>
                                        
                                        <DataTemplate>
                                            <Border
                                                Background="{DynamicResource MeinThemeColor}"
                                                x:Name="brd" 
                                                Width="300" 
                                                MinHeight="400" 
                                                CornerRadius="15 15 10 10">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="50"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>
                                                    <Border 
                                                        x:Name="InBrd"
                                                        Grid.Row="0"
                                                        CornerRadius="10 10 0 0"
                                                        Background="{DynamicResource DimBackgroundColor}">
                                                        <StackPanel
                                                            Orientation="Horizontal"
                                                            HorizontalAlignment="Right"
                                                            >

                                                            <customCtrs:CustomButton
                                                                Grid.Column="0"
                                                                FirstButtonColor="{DynamicResource MeinThemeColor}"
                                                                SecondButtonColor="{DynamicResource DimBackgroundColor}"
                                                                CornerRadius="0 10 0 0"
                                                                ToolTip="Remove file"
                                                                ImageMargin="5"
                                                                Command="{Binding RemoveMediafile}"
                                                                CommandParameter="{Binding}"
                                                                TextBlockHorizontalAlignment ="Left"
                                                                DefoultImageSource="{DynamicResource RoundedCrossContrastinng}"
                                                                MouseHoverImageSource="{DynamicResource RoundedCrossFillContrastinng}"
                                                                
                                                                />
                                          
                                                        </StackPanel>
                                                    </Border>
                                                    <Border
                                                        CornerRadius="0 0 10 10"
                                                        x:Name="ImgBrd"
                                                        Grid.Row="1">
                                                        <Border.Background>
                                                            <ImageBrush 
                                                                Stretch="Fill"
                                                                ImageSource="{Binding MediaImagePath, Converter={StaticResource ToBitmapConverter}}"/>
                                                        </Border.Background>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                            <DataTemplate.Triggers>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsMouseOver" Value="True" />
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="ImgBrd" Property="Margin" Value="25" />
                                                    <Setter TargetName="ImgBrd" Property="CornerRadius" Value="10" />
                                                </MultiTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.Resources>
                                        <Style TargetType="ScrollBar" BasedOn="{StaticResource WhiteScrollBarStyle}"/>
                                    </ListView.Resources>
                                    <ListView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel 
                                                Orientation="Horizontal"  
                                                HorizontalAlignment="Center"
                                                />
                                        </ItemsPanelTemplate>
                                    </ListView.ItemsPanel>
                                </ListView>

                            </Border>

                        </StackPanel>
                    </ScrollViewer>
                </Border>
                <Border 
                    Grid.Column="1"
                    Background="{DynamicResource MeinColorBackgroundFillImage}"
                    Margin="0 20 20 0"
                    >
                    <ScrollViewer
                        PreviewMouseWheel ="ScrollViewer_PreviewMouseWheel"
                        CanContentScroll="False"
                        SnapsToDevicePixels="True"
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Auto"
                        >
                        <ScrollViewer.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource WhiteScrollBarStyle}"/>
                        </ScrollViewer.Resources>
                        <ListView
                        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                        x:Name="FiltersCriterionsList"
                        ScrollViewer.CanContentScroll="False"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        HorizontalContentAlignment="Stretch"
                        ItemContainerStyle="{StaticResource MediaObjactContenerStyle}"
                        ItemsSource="{Binding ExistingFilterCriterions}"
                        BorderThickness="0"
                        Background="Transparent"
                        Padding="0 10 0 0"
                        >
                            <ListView.ItemTemplate>

                                <DataTemplate>

                                    <Border
                                    MinHeight="350"
                                    Background="{DynamicResource DimBackgroundColor}"
                                    CornerRadius="40"
                                    Margin="20"
                                    >
                                        <StackPanel
                                        Orientation="Vertical"
                                        >
                                            <Border 
                                            MinHeight="75"
                                            Background="{DynamicResource MeinThemeColor}"
                                            CornerRadius="10">
                                                <TextBlock
                                                TextWrapping="Wrap"
                                                Foreground="{DynamicResource MeinBackgroundColor}"
                                                FontSize="25"
                                                Style="{StaticResource SpanDefoult}"
                                                TextAlignment="Center"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"
                                                x:Name="FilterCriterionTytleTextBlock"
                                                Text="{Binding Tytle}"
                                                />
                                            </Border>

                                            <ListView
                                            MinHeight="300"
                                            HorizontalContentAlignment="Stretch"
                                            BorderThickness="0"
                                            ItemsSource="{Binding Filters}"
                                            Background="Transparent"
                                            Padding="0 10 20 10"
                                            ItemContainerStyle="{StaticResource RankingListItemStyle}"
                                              >
                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid
                                                        HorizontalAlignment="Stretch"
                                                        >
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="50"/>
                                                            </Grid.RowDefinitions>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="50"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock
                                                            HorizontalAlignment="Left"
                                                            
                                                            Foreground="{Binding 
                                                            Path=Foreground, 
                                                            RelativeSource={RelativeSource AncestorType=ListViewItem,Mode=FindAncestor}}"
                                                            
                                                            Grid.Row="0"
                                                            Grid.Column="0"
                                                            x:Name="TytleTbx"
                                                            FontSize="20"
                                                            TextWrapping="Wrap"
                                                            Background="Transparent"
                                                            Text="{Binding ConditionTytle, Mode=TwoWay}"
                                                            Width="250"
                                                            Height="50"
                                                            Margin="30 0 0 0"
                                                            />
                                                            <Viewbox
                                                            Grid.Row="0"
                                                            Grid.Column="1"
                                                            Width="50"
                                                            Height="50"
                                                            HorizontalAlignment="Right"
                                                            >
                                                                <CheckBox
                                                                BorderBrush="{DynamicResource MeinThemeColor}"
                                                                Background="{DynamicResource MeinBackgroundColor}"
                                                                Style="{StaticResource CheckBoxRoundenStyle}"
                                                            >
                                                                    <i:Interaction.Triggers>
                                                                        <i:EventTrigger EventName="Checked">
                                                                            <i:InvokeCommandAction
                                                                        Command="{Binding DataContext.AddFilterCommand, RelativeSource={RelativeSource AncestorLevel=2,AncestorType=ListView,Mode=FindAncestor}}"
                                                                        CommandParameter="{Binding}"/>
                                                                        </i:EventTrigger>
                                                                        <i:EventTrigger EventName="Unchecked">
                                                                            <i:InvokeCommandAction
                                                                        Command="{Binding DataContext.RemoveFilterCommand, RelativeSource={RelativeSource AncestorLevel=2,AncestorType=ListView,Mode=FindAncestor}}"
                                                                        CommandParameter="{Binding}"/>
                                                                        </i:EventTrigger>
                                                                    </i:Interaction.Triggers>
                                                                </CheckBox>
                                                            </Viewbox>

                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                            </ListView>
                                        </StackPanel>
                                    </Border>

                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel 
                                    Orientation="Vertical"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Stretch"
                                    />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                        </ListView>
                    </ScrollViewer>
                </Border>
            </Grid>

            <customCtrs:CustomButton
                FirstButtonColor="{DynamicResource MeinThemeColor}"
                SecondButtonColor="{DynamicResource MeinBackgroundColor}"
                DisabledStateColor="{DynamicResource DimBackgroundColor}"
                Grid.Row="2"
                CornerRadius="10"
                FontSize="30"
                Height="auto"
                BorderThickness="2"
                Text="Create new ranking category"
                Margin="20"
                ImageMargin="15"
                Command="{Binding CreateMediaObjectCommand}"
                TextBlockHorizontalAlignment ="Center"
                DisabledImageSource="{DynamicResource MarkListBordered2}"
                DefoultImageSource="{DynamicResource MarkListMein}"
                MouseHoverImageSource="{DynamicResource MarkListBordered}"
                />
            
        </Grid>
    </Border>
</customCtrs:BaseWindow>
