<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style 
        BasedOn="{StaticResource {x:Type Button}}"
        x:Key="Button" 
        TargetType="Button">

        <Style.Setters>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                    Name="brd"
                    CornerRadius="10"
                    BorderThickness="2"
                    BorderBrush="{TemplateBinding Background}"
                    Background="White">

                            <Grid VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch">
                                <TextBlock
                            Name="txt"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            FontSize="20"
                            Foreground ="{TemplateBinding Background}"
                            Text="{TemplateBinding Content}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="brd" Property="Background" Value="{Binding ElementName=brd, Path=BorderBrush}"/>
                                <Setter TargetName="brd" Property="BorderThickness" Value="0"/>
                                <Setter TargetName="txt" Property="Foreground" Value="White"/>

                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="txt" Property="Foreground" Value="#ccd5f0"/>
                                <Setter TargetName="brd" Property="BorderBrush" Value="#ccd5f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>

    </Style>
</ResourceDictionary>