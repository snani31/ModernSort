<customCtrs:BaseWindow
        x:Class="ModernSort.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModernSort"
        xmlns:view="clr-namespace:ModernSort.View.Windows"
        xmlns:vms="clr-namespace:ModernSort.ViewModel.Windows"
        xmlns:customCtrs ="clr-namespace:ModernSort.CustomeControls.CustomUIControls"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        WindowStyle="none"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        Title="MainWindow" 
        Height="450" 
        Width="800"
        MouseLeftButtonDown="Window_DragHandler"
        >
    <Border
     Background= "{DynamicResource MeinBackgroundColor}"
     CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=customCtrs:BaseWindow,Mode=FindAncestor},Path=MainBorderCornerRadius}"
     ClipToBounds="True"
         >
        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition 
                 Height="50"
                 />
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border
                Grid.Column="0"
                Grid.ColumnSpan="1"
                Background="{DynamicResource MeinThemeColor}"
                d:CornerRadius="20 20 0 0"
                CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=customCtrs:BaseWindow,Mode=FindAncestor},Path=TopButtonsBarBorderCornerRadius}"    
             >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>


                    <Button Grid.Column="1"
                         ToolTip="Hide"
                            Click="HideButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" Background="Transparent" CornerRadius="10 20 0 10">
                                    <Image Margin="8" Source="{DynamicResource MinimizeBordered}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="{DynamicResource DimMeinThemeColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2"
                         ToolTip="Maximize"
                            Click="MaximizeButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" CornerRadius="10 20 0 10">
                                    <Image Margin="8" Source="{DynamicResource MaximizeBordered}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="{DynamicResource DimMeinThemeColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="3"
                        Command="{Binding CloseApplication}"
                        ToolTip="Close">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderThickness="0" Margin="0" Name="brd" CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=customCtrs:BaseWindow,Mode=FindAncestor},Path=TopRightCloseButtonBorderCornerRadius}">
                                    <Image Margin="10" Source="{DynamicResource PowerOffMeinBackgroundImage}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="brd" Property="Background" Value="{DynamicResource ContrastingColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="2*"
                        />
                    <ColumnDefinition
                        Width="*"/>
                </Grid.ColumnDefinitions>
                <Border
                    Grid.Column="0"
                    Background= "{DynamicResource DimBackgroundColor}"
                    CornerRadius="0,20,20,20"
                    ClipToBounds="True">
                    <Border Background="{DynamicResource DimBackgroundColorFillImage}">
                        <ListView
                    ScrollViewer.CanContentScroll="False"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    HorizontalContentAlignment="Left"
                    BorderThickness="0"
                    ItemsSource="{Binding RankingCategoriesItems}"
                    Background="Transparent"
                    Padding="0 10 0 10"
                      >
                            <ListView.ItemContainerStyle>
                                <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource RankingListItemStyle}">
                                    <Setter Property="ToolTip">
                                        <Setter.Value>
                                            <customCtrs:CustomTooltip
                                                MaxWidth="400"
                                                HeaderText="Description"
                                                ContentText="{Binding Descryption}"
                                                Background="{DynamicResource MeinBackgroundColor}"
                                                Foreground="{DynamicResource MeinThemeColor}"
                                                BorderBrush="{DynamicResource MeinThemeColor}"
                                                Placement="MousePoint"
                                                />
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Margin" Value="0 0 20 0"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid
                                    Background="Transparent"
                                    >
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="150"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="150"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Border
                                        x:Name="ItemBrd"
                                        BorderBrush="{DynamicResource MeinBackgroundColor}"
                                        BorderThickness="5"
                                        ClipToBounds="True"
                                        CornerRadius="20"
                                        >
                                            <Border.Background>
                                                <ImageBrush
                                                    Stretch="Fill"
                                                    ImageSource="{Binding RankingIconPath,
                                                    Converter={StaticResource ToBitmapConverter}}"
                                                    />
                                            </Border.Background>
                                        </Border>
                                        <TextBlock 
                                        Margin="25"
                                        FontSize="30"
                                        FontWeight="DemiBold"
                                        FontFamily="\AppResources\Fonts\#Pribambas"
                                        TextWrapping="Wrap"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        Grid.Column="1"
                                        Grid.Row="0"
                                        Text="{Binding Tytle}"/>
                                        <Button
                                        Grid.Column="0"
                                        Command="{Binding DataContext.OpenEditRankingWindow, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent"
                                        >
                                            <Button.ToolTip>
                                                <customCtrs:CustomTooltip
                                                    MaxWidth="400"
                                                    ContentText="Edit Ranking"
                                                    Background="{DynamicResource MeinBackgroundColor}"
                                                    Foreground="{DynamicResource MeinThemeColor}"
                                                    BorderBrush="{DynamicResource MeinThemeColor}"
                                                    Placement="MousePoint"
                                                    />
                                            </Button.ToolTip>
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border 
                                                    BorderBrush="{DynamicResource ContrastingColor}"
                                                    Name="brd" 
                                                    Background="Transparent"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"
                                                    CornerRadius="20">
                                                        <Image
                                                        x:Name="img"
                                                        Margin="40"
                                                        />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="img" Property="Source" Value="{DynamicResource EditPencelContrasting}"/>
                                                            <Setter TargetName="brd" Property="BorderThickness" Value="{Binding ElementName=ItemBrd, Path=BorderThickness}"/>
                                                            <Setter TargetName="brd" Property="Background" Value="{DynamicResource ContrastingColorOpacity}">
                                                            </Setter>
                                                        </Trigger>

                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="MouseDown">
                                                <i:InvokeCommandAction 
                                                Command="{Binding DataContext.OpenSelectedRankingWindow, 
                                                RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                                                CommandParameter="{Binding}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>

                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource WhiteScrollBarStyle}"/>
                            </ListView.Resources>
                        </ListView>
                    </Border>
                </Border>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <customCtrs:CustomButton
                        BorderThickness="4"
                        BorderColorBrush="Red"
                        FirstButtonColor="{DynamicResource MeinThemeColor}"
                        SecondButtonColor="{DynamicResource MeinBackgroundColor}"
                        DisabledStateColor="{DynamicResource DimBackgroundColor}"
                        FontSize="22"
                        Command="{Binding OpenNewRankingWindow}"
                        TextBlockHorizontalAlignment ="Left"
                        Text="Create new Ranking"
                        ImageMargin="15"
                        CornerRadius="10"
                        DisabledImageSource="{DynamicResource AddRecordBordered2}"
                        DefoultImageSource="{DynamicResource AddRecordMein}"
                        MouseHoverImageSource="{DynamicResource AddRecordBordered}"
                        Margin="20"
                        Grid.Row="0"
                            />

                    <customCtrs:CustomButton
                        BorderColorBrush="Red"
                        FirstButtonColor="{DynamicResource MeinThemeColor}"
                        SecondButtonColor="{DynamicResource MeinBackgroundColor}"
                        DisabledStateColor="{DynamicResource DimBackgroundColor}"     
                        Command="{Binding ChangeUITheme}"
                        BorderThickness="4"
                        Margin="20"
                        FontSize="22"
                        Grid.Row="1"
                        CornerRadius="10"
                        ImageMargin="15"
                        Text="Switch theme"
                        TextBlockHorizontalAlignment ="Left"
                        DisabledImageSource="{DynamicResource PaletteBordered2}"
                        DefoultImageSource="{DynamicResource PaletteMein}"
                        MouseHoverImageSource="{DynamicResource PaletteBordered}"
                          >
                    </customCtrs:CustomButton>

                    <customCtrs:CustomButton
                        BorderColorBrush="Red"
                        FirstButtonColor="{DynamicResource MeinThemeColor}"
                        SecondButtonColor="{DynamicResource MeinBackgroundColor}"
                        DisabledStateColor="{DynamicResource DimBackgroundColor}"
                        Margin="20"
                        Grid.Row="2"
                        ImageMargin="15"
                        FontSize="22"
                        BorderThickness="4"
                        CornerRadius="10"
                        Command="{Binding CloseApplication}"
                        Text="Exit"
                        TextBlockHorizontalAlignment ="Left"
                        DisabledImageSource="{DynamicResource DoorExitBordered2}"
                        DefoultImageSource="{DynamicResource DoorExitMein}"
                        MouseHoverImageSource="{DynamicResource DoorExitBordered}"
                            >
                    </customCtrs:CustomButton>
                </Grid>
            </Grid>



        </Grid>

    </Border>
</customCtrs:BaseWindow>
